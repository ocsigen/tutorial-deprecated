TODO List
 * complete urls for each module


COMPILATION
 * compile on mindmap:
    make <name>
      for <name> in {services, content, communication, utils, caption}
 * compile all images:
    make all